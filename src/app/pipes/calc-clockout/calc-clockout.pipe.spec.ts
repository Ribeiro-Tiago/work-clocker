import { CalcClockoutPipe } from './calc-clockout.pipe';

describe('CalcClockoutPipe', () => {
	it('create an instance', () => {
		const pipe = new CalcClockoutPipe();
		expect(pipe).toBeTruthy();
	});
});
